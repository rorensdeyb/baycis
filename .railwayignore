# Kept in git, skipped in the Railway upload (same idea as DCore's .railwayignore).
# Local SQLite files must never be deployed — Railway uses Postgres.
*.sqlite*
*.sqlite-journal
# Standalone nested scaffold, not needed at runtime.
testserve/
# Rebuilt on Railway during the build phase.
node_modules/
vendor/
public/hot
# Local/dev artefacts.
*.log
.md/
